{
  "name": "graph-emulator-playwright-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "pw:install": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
